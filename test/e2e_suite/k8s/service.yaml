apiVersion: v1
kind: Service
metadata:
  name: '{{.Pkg.Name}}'
  labels:
    app: '{{.Pkg.Name}}'
spec:
  selector:
    app: '{{.Pkg.Name}}'
  ports:
  - protocol: TCP
    port: 80
    targetPort: 9376
